# Todo List Parser

The basic idea of ​​this parser is that it takes a different row as input, processes what the actual execution status is ([x] or [ ]), and changes the structuring of the TaskStatus result.

## Example